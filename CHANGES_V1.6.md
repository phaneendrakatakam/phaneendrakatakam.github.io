# V1.6 Header Polish

- Added rounded corners to the sticky top navigation.
- Added safe left/right padding so the `PK` mark and `Let's Connect` button no longer touch or overflow the edges.
- Reduced desktop header gap slightly for better breathing room.
- Added a subtle border and shadow so the header feels like a deliberate floating navigation bar.
- Added responsive header spacing for tablet and mobile.
- Bumped CSS/JS cache-busting version to `1.6`.
